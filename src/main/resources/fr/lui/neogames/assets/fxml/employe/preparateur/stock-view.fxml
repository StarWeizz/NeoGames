<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<VBox xmlns:fx="http://javafx.com/fxml/1" xmlns="http://javafx.com/javafx/17.0.12"
      fx:controller="fr.lui.neogames.controllers.employe.preparateur.StockController"
      spacing="10" style="-fx-padding: 20;">

    <!-- Titre -->
    <Label text="📦 Gestion du Stock" style="-fx-font-size: 18px; -fx-font-weight: bold;" />

    <!-- Info -->
    <Label text="Gérez le stock de tous les jeux du catalogue"
           style="-fx-font-size: 12px; -fx-text-fill: #666;" />

    <!-- TableView du stock -->
    <TableView fx:id="tableStock" VBox.vgrow="ALWAYS">
        <columns>
            <TableColumn fx:id="colNom" text="Nom" prefWidth="200" />
            <TableColumn fx:id="colEditeur" text="Éditeur" prefWidth="120" />
            <TableColumn fx:id="colPlateforme" text="Plateforme" prefWidth="100" />
            <TableColumn fx:id="colPrix" text="Prix" prefWidth="80" />
            <TableColumn fx:id="colStock" text="Stock" prefWidth="80" />
            <TableColumn fx:id="colOccasion" text="Occasion" prefWidth="80" />
        </columns>
    </TableView>

    <!-- Actions -->
    <HBox spacing="10" alignment="CENTER_LEFT">
        <Label text="Quantité :" />
        <Spinner fx:id="spinnerQuantite" initialValue="1" min="0" max="999" prefWidth="80" editable="true" />

        <Button text="➕ Ajouter stock" onAction="#ajouterStock"
                style="-fx-background-color: #27ae60; -fx-text-fill: white;" />
        <Button text="➖ Retirer stock" onAction="#retirerStock"
                style="-fx-background-color: #e74c3c; -fx-text-fill: white;" />

        <Region HBox.hgrow="ALWAYS" />

        <Button text="🔄 Rafraîchir" onAction="#rafraichir"
                style="-fx-background-color: #34495e; -fx-text-fill: white;" />
    </HBox>

</VBox>
